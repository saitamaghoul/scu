[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# If you really want "no dashboard env vars", edit this file once and commit it.
# Replace the placeholder with your Render backend URL.
[build.environment]
  VITE_API_BASE_URL = "https://scu-y0nk.onrender.com"

